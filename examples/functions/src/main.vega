// Your entry point
fun main() {
    println("1 + 1 = ${add(1, 2)}")
    println(apply("hello", fun (str) = str + "!"))
}

fun add(a: Int32, b: Int32): Int32 {
    return a + b
}

fun do_nothing<T>(value: T): T = value

fun apply<T, U>(value: T, f: Callable<(T), U>): U {
    return f(value)
}
