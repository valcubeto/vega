use args::flags::RawArgs;
use terminal::debug;
use super::init::init_project;

pub fn create_project(args: &RawArgs) {
    debug!("Creating project with args {args:?}");
    todo!();
    // let path = create_folder();
    // init_project(path)
}
