use prelude::types::*;
use prelude::terminal::*;
use super::vega_init::init_project;

pub fn create_project(args: SliceView<Str>) {
    debug!("Creating project with args {args:?}");
    todo!();
    // let path = create_folder();
    // init_project(path)
}
